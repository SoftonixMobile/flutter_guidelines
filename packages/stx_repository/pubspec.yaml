name: stx_repository
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ^3.13.0

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0
  meta: ^1.19.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.32.0
